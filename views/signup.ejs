<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>sign up</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500&display=swap');
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      background-color: #f5f0e8;
      font-family: 'Cormorant Garamond', Georgia, serif;
      color: #5c4a3d;
      position: relative;
      overflow: hidden;
    }
    
    .sparkle {
      position: absolute;
      font-size: 1.5rem;
      color: #1a1a1a;
      opacity: 0.6;
    }
    
    .sparkle-1 { top: 10%; left: 5%; }
    .sparkle-2 { top: 25%; left: 8%; }
    .sparkle-3 { top: 45%; left: 3%; }
    .sparkle-4 { top: 65%; left: 6%; }
    .sparkle-5 { top: 80%; left: 4%; }
    .sparkle-6 { top: 15%; right: 5%; }
    .sparkle-7 { top: 35%; right: 7%; }
    .sparkle-8 { top: 55%; right: 4%; }
    .sparkle-9 { top: 75%; right: 6%; }
    .sparkle-10 { top: 90%; right: 5%; }
    
    main {
      text-align: center;
      padding: 2rem;
      max-width: 400px;
      width: 100%;
    }
    
    h1 {
      font-size: clamp(2rem, 8vw, 4rem);
      font-weight: 300;
      letter-spacing: 0.15em;
      margin-bottom: 0.5rem;
      color: #4a3c31;
    }
    
    .subtitle {
      font-size: clamp(0.9rem, 2vw, 1rem);
      font-weight: 400;
      letter-spacing: 0.25em;
      text-transform: lowercase;
      opacity: 0.6;
      margin-bottom: 2.5rem;
    }
    
    .line {
      width: 60px;
      height: 1px;
      background-color: #8b7355;
      margin: 1.5rem auto;
      opacity: 0.4;
    }
    
    form {
      display: flex;
      flex-direction: column;
      gap: 1.25rem;
    }
    
    .input-group {
      text-align: left;
    }
    
    label {
      display: block;
      font-size: 0.85rem;
      letter-spacing: 0.2em;
      text-transform: lowercase;
      margin-bottom: 0.5rem;
      opacity: 0.7;
    }
    
    input {
      width: 100%;
      padding: 0.875rem 1rem;
      border: 1px solid rgba(139, 115, 85, 0.3);
      background-color: rgba(255, 255, 255, 0.5);
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 1rem;
      color: #4a3c31;
      outline: none;
      transition: border-color 0.3s ease;
    }
    
    input::placeholder {
      color: #8b7355;
      opacity: 0.5;
    }
    
    input:focus {
      border-color: rgba(139, 115, 85, 0.6);
    }
    
    button {
      margin-top: 1rem;
      padding: 1rem 2rem;
      background-color: #4a3c31;
      color: #f5f0e8;
      border: none;
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 1rem;
      letter-spacing: 0.2em;
      text-transform: lowercase;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }
    
    button:hover {
      background-color: #5c4a3d;
      transform: translateY(-2px);
    }
    
    .footer-text {
      margin-top: 2rem;
      font-size: 0.9rem;
      opacity: 0.6;
    }
    
    .footer-text a {
      color: #5c4a3d;
      text-decoration: none;
      border-bottom: 1px solid #5c4a3d;
      transition: all 0.3s ease;
    }
    
    /* Error message styling */
    .error-message {
      color: #e74c3c;
      background-color: #fde8e8;
      border: 1px solid #f5c6cb;
      padding: 12px 20px;
      border-radius: 4px;
      margin-bottom: 20px;
      font-size: 14px;
      text-align: center;
    }
    
    /* Style for input error state */
    input:invalid {
      border-color: #e74c3c;
    }
    
    .footer-text a:hover {
      border-color: rgba(74, 60, 49, 0.8);
    }
  </style>
</head>
<body>
  <!-- Left sparkles -->
  <span class="sparkle sparkle-1">✦</span>
  <span class="sparkle sparkle-2">✧</span>
  <span class="sparkle sparkle-3">✦</span>
  <span class="sparkle sparkle-4">✧</span>
  <span class="sparkle sparkle-5">✦</span>
  
  <!-- Right sparkles -->
  <span class="sparkle sparkle-6">✧</span>
  <span class="sparkle sparkle-7">✦</span>
  <span class="sparkle sparkle-8">✧</span>
  <span class="sparkle sparkle-9">✦</span>
  <span class="sparkle sparkle-10">✧</span>
  
  <main>
    <h1>join us</h1>
    <p class="subtitle">create your account</p>
    
    <form   method="POST"action="/signup">
      <div class="input-group">
        <label for="name">name</label>
        <input type="text" id="name" name="name" placeholder="your name" value="<%= typeof formData !== 'undefined' ? formData.name : '' %>" required>
      </div>
      
      <div class="input-group">
        <label for="email">email</label>
        <input type="email" id="email" name="email" placeholder="your@email.com" value="<%= typeof formData !== 'undefined' ? formData.email : '' %>" required>
      </div>
      
      <div class="input-group">
        <label for="password">password</label>
        <input type="password" id="password" name="password" placeholder="••••••••" required>
      </div>
      
      <button type="submit">sign up</button>
    </form>
    
    <div class="line"></div>
    
    <p class="footer-text">already have an account? <a href="#">sign in</a></p>
  </main>
</body>
</html>
